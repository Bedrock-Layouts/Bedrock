import { InlineCluster } from '@bedrock-layout/inline-cluster';
import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs/blocks';

import { Box } from './Box';

<Meta title="Components/InlineCluster" component={InlineCluster} />

# InlineCluster

## Use case

The InlineCluster component is designed consistent spacing around inline elements of variable width

## How to install

If you haven't already, install `styled-components`:

```bash
  npm install styled-components
```

Then install the InlineCluster component

```bash
  npm install @bedrock-layout/inline-cluster
```

Finally, you import the component.

```javascript
import { InlineCluster } from '@bedrock-layout/inline-cluster';
```

## API

<ArgsTable of={InlineCluster} />

## Basic usage

<Preview withToolbar>
  <Story name="Basic">
    <InlineCluster gutter="lg">
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
    </InlineCluster>
  </Story>
</Preview>

## With x-large gutter

<Preview withToolbar>
  <Story name="With x-large gutter">
    <InlineCluster gutter="xl">
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
    </InlineCluster>
  </Story>
</Preview>

## With center justify

<Preview withToolbar>
  <Story name="With center justify">
    <InlineCluster justify="center" gutter="xl">
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
    </InlineCluster>
  </Story>
</Preview>

## With center align

<Preview withToolbar>
  <Story name="With center align">
    <InlineCluster align="center" gutter="xl">
      <Box style={{ height: 200 }} />
      <Box />
      <Box />
      <Box />
    </InlineCluster>
  </Story>
</Preview>

## data-attribute

For purposes of styling, you can select `data-bedrock-layout-inline-cluster`.

## Playground

Experiment with this story with the Controls addon in the Canvas tab.

export const Template = (args) =>  (
   <InlineCluster {...args}>
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
    </InlineCluster>
);

<Canvas withToolbar>
  <Story
    name="Controls"
    args={{
      gutter: "lg",
      justify: "start",
      align: "start",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Controls" />
