import { Inline } from '@bedrock-layout/inline';
import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs/';

import { Box } from './Box';
import { argTypes } from './argTypes'

<Meta title="Components/Inline" component={Inline} />

# Inline

## Use case

The `Inline` component is designed to create consistent spacing around inline elements of variable width. Unlike the `InlineCluster` component,
the items in the `Inline` component will not wrap. The `Inline` component also allows you to specify a component that can stretch and fill the excess space.

## Installation

```bash
yarn add @bedrock-layout/inline
```

## API

<Story name="API" argTypes={argTypes} />

<ArgsTable story="API" />

## InlineCluster Props

The `Inline` component has all the same props as the `InlineCluster` component. (check the [InlineCluster component for more details](/?path=/docs/components-inlinecluster--api))

Below is an example of the `Inline` component with the `gutter` prop set to `xl` and both `justify` and `align` props set to `center`.

<Canvas withToolbar withSource="open">
  <Story name="InlineCluster Props">
    <Inline gutter="xl" justify="center" align="center">
      <Box style={{ height: 200 }} />
      <Box />
      <Box />
      <Box />
    </Inline>
  </Story>
</Canvas>

## stretch

The `stretch` prop can be used to specify a child component that will stretch to fill the excess space.

<Canvas withToolbar withSource="open">
  <Story name="stretch">
    <h3>start</h3>
    <Inline gutter="lg" stretch="start">
      <Box />
      <Box />
      <Box />
      <Box />
    </Inline>
    <h3>end</h3>
    <Inline gutter="lg" stretch="end">
      <Box />
      <Box />
      <Box />
      <Box />
    </Inline>
    <h3>all</h3>
    <Inline gutter="lg" stretch="all">
      <Box />
      <Box />
      <Box />
      <Box />
    </Inline>
    <h3>2 index</h3>
    <Inline gutter="lg" stretch={2}>
      <Box />
      <Box />
      <Box />
      <Box />
    </Inline>
  </Story>
</Canvas>

## switchAt

In the below example, the layout will switch to stack when its width becomes less than `45rem`. (Resize your window to see this in action.)

<Canvas withToolbar withSource="open">
  <Story name="switchAt">
    <Inline gutter="lg" switchAt="45rem">
      <Box />
      <Box />
      <Box />
      <Box />
    </Inline>
  </Story>
</Canvas>

## data-attribute

For styling purposes, you can select `data-bedrock-inline`.

## Playground

Experiment with this story with the Controls addon in the Canvas tab.

export const Template = (args) =>  (
   <Inline {...args}>
      <Box />
      <Box />
      <Box />
      <Box />
    </Inline>
);

<Canvas withToolbar>
  <Story
    name="Playground"
    args={{
      gutter: "lg",
      justify: "start",
      align: "start",
      stretch: "end",
      switchAt: "40rem",
    }}
    argTypes={argTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
