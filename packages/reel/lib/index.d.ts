export interface ReelProps {
    maxHeight?: number;
    maxWidth?: number;
    snapX?: boolean;
    proximity?: boolean;
}
declare const Reel: import("styled-components").StyledComponent<"div", any, ReelProps, never>;
export default Reel;
