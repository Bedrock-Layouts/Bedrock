import { Reel } from '../src/index';
import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs/';

import { colors, ColoredRect, argTypes} from './colors'

<Meta title="Spacer Components/Reel" component={Reel} />

# Reel

## Use case

Scrolling is a popular and natural way to interact with web content. The `Reel` component is
designed to organize content into scrollable flatlists with convenient scroll breakpoints.

## Installation

```bash
yarn add @bedrock-layout/reel
```

## API

<Story name="API" argTypes={argTypes} />

<ArgsTable story="API" />

## snapType

The `snapType` prop sets the scroll snap type.

<Canvas withSource="open">
  <Story name="snapType">
    <h3>none</h3>
    <p>scroll to the right to see the next item</p>
    <Reel snapType="none" gutter="lg">
      {colors.map((color, i) => {
        return (
          <ColoredRect key={i} bgColor={color} style={{ minWidth: "100%" }}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>mandatory</h3>
    <p>scroll to the right to see the next item</p>
    <Reel snapType="mandatory" gutter="lg">
      {colors.map((color, i) => {
        return (
          <ColoredRect key={i} bgColor={color} style={{ minWidth: "100%" }}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>proximity</h3>
    <p>scroll to the right to see the next item</p>
    <Reel snapType="proximity" gutter="lg">
      {colors.map((color, i) => {
        return (
          <ColoredRect key={i} bgColor={color} style={{ minWidth: "100%" }}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
  </Story>
</Canvas>

## gutter

The `gutter` prop defines the gutter size between elements. Bedrock has implemented a default spacing scheme, but [it can be overridden using the ThemeProvider provided by `styled-components`.](/docs/spacing--page#integrating-with-your-design-system)
You can also use any valid CSSLength or positve integer.

Here are the possible values for `gutter` by default:

<Canvas withSource="open">
  <Story name="gutter">
    <h3>Custom gutter as number (20)</h3>
    <Reel snapType="none" gutter={20}>
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>Custom gutter as string ("3ch")</h3>
    <Reel snapType="none" gutter="3ch">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>none</h3>
    <Reel snapType="none" gutter="none">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>xxs</h3>
    <Reel snapType="none" gutter="xxs">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>xs</h3>
    <Reel snapType="none" gutter="xs">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>sm</h3>
    <Reel snapType="none" gutter="sm">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>md</h3>
    <Reel snapType="none" gutter="md">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>mdLg</h3>
    <Reel snapType="none" gutter="mdLg">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>lg</h3>
    <Reel snapType="none" gutter="lg">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>lgXl</h3>
    <Reel snapType="none" gutter="lgXl">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>xl</h3>
    <Reel snapType="none" gutter="xl">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>xlXXl</h3>
    <Reel snapType="none" gutter="xlXXl">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
    <h3>xxl</h3>
    <Reel snapType="none" gutter="xxl">
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.{" "}
          </ColoredRect>
        );
      })}
    </Reel>
  </Story>
</Canvas>

## data-attribute

For styling purposes, you can select `data-bedrock-reel`.

## Playground

Experiment with this story with the Controls addon in the Canvas tab.

export const Template = (args) =>  (
    <Reel  {...args}>
      {colors.map((color, ind) => {
        return (
          <ColoredRect key={ind} bgColor={color}>
            Lorem ipsum dolor sit amet.
          </ColoredRect>
        );
      })}
    </Reel>
);

<Canvas>
  <Story
    name="Playground"
    argTypes={argTypes}
    args={{ gutter: "lg", snapType: "mandatory" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
