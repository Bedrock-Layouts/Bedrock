import { Meta, Story, Canvas, ArgsTable, Preview, } from '@storybook/addon-docs/';
import { Component } from './Component';

import "../src/components/split.css"

<Meta title="Bedrock Layout CSS/split.css" parameters={{ viewMode: "docs" }} />

# split.css

This is a CSS Only version of the [Split package](/?path=/docs/components-split--api).

## Use case

The Split component is designed to create a split layout based on a fractional ratio. The Split will enforce a standard spacing scheme through the gutter prop.

## Usage

First you need to import the appropriate stylesheet. The split is part of the main CSS bundle or you can import it in individually from the components directory.

```javascript
import '@bedrock-layout/css/lib/bedrock-layout.min.css';

// or

import "@bedrock-layout/css/lib/components/split.min.css";
```

Then you add the `data-bedrock-split` attribute to the desired element.

## --gutter

The `--gutter` prop defines the gutter size between elements. Bedrock has implemented a [default spacing scheme](css-only-components-spacing-properties-css--page), or you can simply pass any valid CSS length or percentage value.

You can adjust the space between the child elements by setting the `--gutter` property in the inline style.

<Canvas withSource="open">
  <Story name="--gutter">
    <h3>Spacing Property such as open-props</h3>
    <div data-bedrock-split style={{ "--gutter": "var(--size-2)" }}>
      <Component />
      <Component />
    </div>
    <h3>Any CSS Length</h3>
    <div data-bedrock-split style={{ "--gutter": "2rem" }}>
      <Component />
      <Component />
    </div>
  </Story>
</Canvas>

## fraction

The `fraction` prop defines the fraction of the container width to use for the split.

By default the split will be a `1/2` split. You can change this by setting the `fraction:` value on the `data-bedrock-split` attribute.

<Canvas withSource="open">
  <Story name="fraction">
    <h3>1/4</h3>
    <div data-bedrock-split="fraction:1/4" style={{ "--gutter": "1rem" }}>
      <Component />
      <Component />
    </div>
    <h3>1/3</h3>
    <div data-bedrock-split="fraction:1/3" style={{ "--gutter": "1rem" }}>
      <Component />
      <Component />
    </div>
    <h3>1/2</h3>
    <div data-bedrock-split="fraction:1/2" style={{ "--gutter": "1rem" }}>
      <Component />
      <Component />
    </div>
    <h3>2/3</h3>
    <div data-bedrock-split="fraction:2/3" style={{ "--gutter": "1rem" }}>
      <Component />
      <Component />
    </div>
    <h3>3/4</h3>
    <div data-bedrock-split="fraction:3/4" style={{ "--gutter": "1rem" }}>
      <Component />
      <Component />
    </div>
    <h3>auto-start</h3>
    <p>(The first Component has a width of 100px)</p>
    <div
      data-bedrock-split="fraction:auto-start"
      style={{ "--gutter": "1rem" }}
    >
      <Component />
      <Component />
    </div>
    <h3>auto-end</h3>
    <p>(The second Component has a width of 100px)</p>
    <div data-bedrock-split="fraction:auto-end" style={{ "--gutter": "1rem" }}>
      <Component />
      <Component />
    </div>
  </Story>
</Canvas>

## --minItemWidth

The `--minItemWidth` prop defines the minimum inline size of each child.

If the minimum inline size can not be maintained, it will move to a stacking layout.

(Resize window to observe the changes)

<Canvas withSource="open">
  <Story name="--minItemWidth">
    <h3>With fraction:2/3 and minItemWidth of 40ch</h3>
    <div
      data-bedrock-split="fraction:2/3"
      style={{ "--gutter": "var(--size-2)", "--minItemWidth": "40ch" }}
    >
      <Component />
      <Component />
    </div>
    <h3>With auto-start and minItemWidth of 30ch</h3>
    <div
      data-bedrock-split="fraction:auto-start"
      style={{ "--gutter": "var(--size-2)", "--minItemWidth": "30ch" }}
    >
      <Component />
      <Component />
    </div>
  </Story>
</Canvas>

## --switchAt

If you set the `--switchAt` property, the inline will switch to a stacking layout if the width of the element is below the threshold set by the `--switchAt` property.

This can be used with `--minItemWidth` and it will switch to a stacking layout at which ever size is largest.

The below example will switch to a stack when it is less than `45rem`.

(Resize your window to see this in action.)

<Canvas withSource="open">
  <Story name="--switchAt">
    <h3>With fraction:3/4</h3>
    <div
      data-bedrock-split="fraction:2/3"
      style={{ "--gutter": "var(--size-2)", "--switchAt": "45rem" }}
    >
      <Component />
      <Component />
    </div>
    <h3>With auto-start with 20rem minItemWidth</h3>
    <div
      data-bedrock-split="fraction:auto-start"
      style={{
        "--gutter": "var(--size-2)",
        "--minItemWidth": "20rem",
        "--switchAt": "45rem",
      }}
    >
      <Component />
      <Component />
    </div>
  </Story>
</Canvas>
