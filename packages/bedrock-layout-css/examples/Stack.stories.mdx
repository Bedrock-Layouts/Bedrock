import { Meta, Story, Canvas, ArgsTable, Preview, } from '@storybook/addon-docs/';
import { Component } from './Component';

import "../src/components/stack.css"
import "../src/components/spacing-properties.css"

<Meta title="CSS Only/stack.css" parameters={{ viewMode: "docs" }} />

# stack.css

This is a CSS Only version of the [Stack package](/?path=/docs/components-stack--api).

## Use case

The Stack is designed to literally 'stack' items on top of each other while maintaining a consistent gutter between each item.

## Usage

First you need to import the appropriate stylesheet. The stack is part of the main CSS bundle or you can import it in individually from the components directory.

```javascript
import '@bedrock-layout/bedrock-layout-css/lib/bedrock-layout.css';

// or

import "@bedrock-layout/bedrock-layout-css/lib/components/stack.css";
```

Then you add the `data-bedrock-stack` attribute to the desired element.

## --gutter

The `--gutter` prop defines the gutter size between elements. Bedrock has implemented a [default spacing scheme](css-only-components-spacing-properties-css--page), or you can simply pass any valid CSS length or percentage value.

You can adjust the space between the child elements by setting the `--gutter` property in the inline style.

<Canvas withSource="open">
  <Story name="--gutter">
    <h3>Bedrock Layout Spacing Property</h3>
    <div data-bedrock-stack style={{ "--gutter": "var(--space-md)" }}>
      <Component />
      <Component />
      <Component />
      <Component />
    </div>
    <h3>Any CSS Length</h3>
    <div data-bedrock-stack style={{ "--gutter": "2rem" }}>
      <Component />
      <Component />
      <Component />
      <Component />
    </div>
  </Story>
</Canvas>
