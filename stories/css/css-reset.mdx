import { Meta } from "@storybook/addon-docs";

<Meta title="Bedrock Layout CSS/reset.css" parameters={{ viewMode: "docs" }} />

# CSS Reset

A CSS Reset that compliments well with the Bedrock Layout Primitives

The reset is not included in the main CSS bundle, so you will need to bring it in individually.

```javascript
import "@bedrock-layout/css/lib/reset.min.css";
```
