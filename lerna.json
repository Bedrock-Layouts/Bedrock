{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "independent",
  "useNx": "true",
  "command": {
    "version": {
      "conventionalCommits": true,
      "allowBranch": ["main"]
    },
    "publish": {
      "conventionalCommits": true,
      "allowBranch": ["main"],
      "yes": true
    }
  }
}
